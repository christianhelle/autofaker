name: mac

on:
  push:
  workflow_dispatch:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    uses: christianhelle/autofaker/.github/workflows/build.yml@main
    with:
      os: macos-latest
